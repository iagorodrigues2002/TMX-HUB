# Global Railway defaults applied to every service in this project.
# Per-service configuration (which Dockerfile, which env vars) lives in
# the Railway dashboard under each service's settings.

[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
